define([    'my-config',    'jquery',    'image!/img/sp_ship.png'],
function (   MyConfig,       $,           ImageShip) {

    // The Start Server is responsible for handling login and registration
    // 
    var Images {
        ship    : new ImageShip()
    };
    return Images;
});

